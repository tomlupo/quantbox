{
  "$comment": [
    "The one per-repo qute policy surface (qute-essentials scripts/qute_config.py).",
    "Sections are namespaced; an ABSENT section means 'this repo says nothing' and",
    "the caller applies its own derived default — absence is never 'off'.",
    "",
    "`git` and `worktree` moved here from .claude/git-guard.json and",
    ".claude/worktree.json. The in-tree pre-push hook was re-vendored in the SAME",
    "change: the older copy read .claude/git-guard.json ONLY, so folding the",
    "section without replacing the hook would have made it see 'repo not opted in'",
    "and wave every push to `main` through silently.",
    "",
    "On the `git` section (it carries no $comment of its own: the guard warns",
    "about any field it does not recognise). One key, two consumers — the",
    "pre-push guard reads `integration_branch` as 'this branch is guarded too',",
    "and /ship reads it to decide the repo is two-stage. It is declared for the",
    "stricter reading; see CLAUDE.md '## Git workflow'. `protected_branch` is",
    "left unset because the guard's default is `main`, which is what this repo",
    "means."
  ],

  "git": {
    "integration_branch": "dev",
    "release_tool": "commitizen (/ship)"
  },

  "worktree": {
    "base_path": "$HOME/workspace/projects/.worktrees/{project}-{slug}",
    "branch_pattern": "{type}/{slug}",
    "default_type": "feat",
    "venv_setup": "uv",
    "base_branch": "dev"
  },

  "ship": {
    "$comment": [
      "/ship finishes at 'the tag is pushed' and then asks the repo what the",
      "after-step is. For quantbox there is NO deploy command: prod does not pull",
      "this repo at all. quantbox-live PINS a tag, and its cron picks the pin up",
      "(scripts/run_daily.sh does `git merge origin/main` + `uv sync` at 06:00 UTC).",
      "",
      "So the after-step is a VERIFICATION and a hand-off, not a deploy — the",
      "script checks the tag is an ancestor of main, reports the pin quantbox-live",
      "currently declares, and prints what remains. It changes nothing."
    ],
    "after_release": "./scripts/after-release.sh"
  },

  "review": {
    "$comment": [
      "base_branch is load-bearing: without it /quant-review falls back to",
      "origin/HEAD, which resolves to main here — so every review of a feature",
      "branch would diff the whole of dev as if it were the change under review."
    ],
    "base_branch": "dev"
  }
}
