# qute PR-flow policy for quantbox — single source of truth, read by BOTH the
# CI review-gate (.github/workflows/review-gate.yml) and the client-side
# qute-essentials hooks + /qute-coder skill. Agents author PRs as qute-coder[bot];
# the independent-reviewer gate is satisfied by qute-review[bot] (author != reviewer).
assignTo: tomlupo
independentReview: true
allowAgentSelfMerge: false
enforce: false
